1. các chi tiết cần làm rõ trong báo cáo khóa luận:
- mức độ thông tin về data cần công bố: có cần => xem lại phân phối của các biến gen ra đã hợp lý chưa (ok rồi)
- tại sao với kết quả hiện tại ebm lại cho ra kết quả tốt hơn là log reg 
- cần phải so sánh gì (gini, KS, Calibration, Stability / PSI, Lift / Gain chart)
- mức độ đồng bộ trong các bước làm để kết quả so sánh được đồng nhất: dataset, cách xử lý, ...

2. các chi tiết cần sửa trong code 
- ebm:
+ cross validation: có nên áp dung cv cho baseline không (ko)

+ chưa chạy tối ưu, mới là baseline với lr = 1. Nếu chạy tối ưu, cần xác định được 
subsampling để chạy tối ưu trên tập đó 
các biến sử dụng để tìm tham số tối ưu 
khoảng giá trị để chạy tối ưu

+ xem có cần quy định tất cả là monotonic không, hay vẫn cho phép phi tuyến (Chỉ nên ép monotonic cho các biến có lý thuyết rủi ro rõ ràng)

+ ở các segment:
nếu trong model cuối không có các biến để segment thì có sao không  (ko sao)

- các markdown, chú thích sửa lại cho cẩn thận 










- có tunning cho logreg ko
- nếu trong model cuối không có các biến để segment thì có sao không
-  xem có cần quy định tất cả là monotonic không, hay vẫn cho phép phi tuyến, cate thì sao 
Population Default Rate: lấy ở toàn bộ population  
SDR: lấy trong tập train 
sử dụng stratified: tách bad ở train và test cũng bằng nhau
